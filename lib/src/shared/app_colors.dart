import 'package:flutter/material.dart';

class AppColors {
  final Color initialPrimaryColor = const Color.fromRGBO(40, 32, 60, 1);
  final Color initialSecondaryColor = Colors.white;
  final Color initialTertiaryColor = const Color.fromRGBO(32, 32, 32, 1);

  final Color errorContainerBackgroundColor =
      const Color.fromARGB(255, 235, 102, 102);
}
